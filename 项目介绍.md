# 智能环境监测系统

## 项目简介

本项目是一个基于物联网架构的**智能环境监测系统**，实现环境数据实时采集、云端存储、阈值报警与远程控制功能。系统由 STM32 采集端、ESP32 桥接端、OneNET 云平台和微信小程序四部分组成，形成完整的"端—云—端"数据闭环。

### 核心功能

- **环境数据采集**：温度、湿度、光照强度、超声波距离 4 路传感器
- **自动报警**：蜂鸣器 + OLED 显示，6 项阈值可配置（温湿度上下限/光照下限/接近距离）
- **执行器控制**：LED 照明灯、SG90 舵机窗帘、TB6612 直流电机风扇，支持自动/手动模式切换
- **数据云端同步**：ESP32 UART→MQTT 桥接，将数据上报至 OneNET 云平台
- **微信小程序端**：4 页面（数据看板、设备控制、阈值设置、我的），实时查看与远程操控
- **本地持久化**：W25Q64 Flash 存储传感器历史数据 + 阈值参数

---

## 系统架构

```
┌─────────────────────┐    UART     ┌───────────────────┐    WiFi/MQTT    ┌──────────────┐
│   STM32F103C8T6      │◄──────────►│    ESP32-S3         │◄──────────────►│   OneNET 云   │
│   (FreeRTOS)         │  115200    │   (ESP-IDF)        │                │  (MQTT Broker)│
│                      │   JSON     │                    │                │              │
│  ┌─────────────────┐ │            │ ┌────────────────┐ │                │              │
│  │ SHT20 温湿度     │ │            │ │ UART → MQTT 桥接 │ │                │              │
│  │ HC-SR04 超声波   │ │            │ │ NVS 阈值存储    │ │                │              │
│  │ 光敏电阻         │ │            │ └────────────────┘ │                │              │
│  │ OLED 0.96" 显示  │ │            └───────────────────┘                └──────┬───────┘
│  │ 蜂鸣器 + LED     │ │                                                        │
│  │ W25Q64 Flash     │ │                                            ┌───────────┴───────────┐
│  │ 编码器 + 按键    │ │                                            │   微信小程序            │
│  │ 舵机 + 电机 + LED │ │                                            │   wx72718b58b055cbbb   │
│  └─────────────────┘ │                                            │                        │
└─────────────────────┘                                            │ ┌────────────────────┐ │
                                                                    │ │ 数据看板 (仪表盘)   │ │
                                                                    │ │ 设备控制 (开关)     │ │
                                                                    │ │ 阈值设置 (滑块)     │ │
                                                                    │ │ 我的 (总览)        │ │
                                                                    │ └────────────────────┘ │
                                                                    └────────────────────────┘
```

---

## 技术栈

### 硬件

| 组件 | 型号/参数 | 接口 | 用途 |
|------|----------|------|------|
| 主控 MCU | STM32F103C8T6 | — | 传感器采集、执行器控制、OLED 显示 |
| 通信模块 | ESP32-S3 | — | WiFi + MQTT 桥接，连接云平台 |
| 温湿度传感器 | SHT20 | I2C2 (PB10/PB11) | 温度 (-40~125°C) + 湿度 (0~100%) |
| 超声波传感器 | HC-SR04 | GPIO (PA0 Trig, PA1 Echo) | 测距 2~400cm，TIM2 CH2 输入捕获 |
| 光敏传感器 | 光敏电阻 | ADC1_IN2 (PA2) | DMA 循环采集 64 样本均值滤波 |
| OLED 显示屏 | 0.96" 128×64 | I2C (PB6/PB7) | 菜单界面，实时数据展示 |
| 旋转编码器 | EC11 | GPIO (PB12/PB13/PB14) | 菜单导航与参数调节 |
| 按键 | 3× 轻触按键 | GPIO | KEY1/KEY2/KEY3 操作 |
| 蜂鸣器 | 有源蜂鸣器 | PA8 | 报警声响 |
| LED | 5mm LED | PB5 | 照明指示 |
| 舵机 | SG90 | PA3 (TIM2_CH4 PWM) | 电动窗帘 |
| 直流电机 | TB6612 驱动 | PA11 PWM + PB7/PB8 GPIO | 散热风扇 |
| Flash 存储 | W25Q64 (8MB) | SPI2 | 历史数据 + 阈值参数保存 |

### 软件

| 层级 | 技术 | 说明 |
|------|------|------|
| STM32 RTOS | FreeRTOS (CMSIS_V2 API) | 8 个任务：传感器采集、OLED 显示、按键输入、数据分析、报警、存储、通信监听、UART 接收 |
| STM32 HAL | STM32F1xx HAL 库 | 外设驱动：I2C、SPI、ADC、TIM、UART、GPIO |
| STM32 中间件 | ARM CMSIS-DSP / NN | 数字信号处理库 |
| ESP32 RTOS | ESP-IDF v5.5.4 (FreeRTOS) | WiFi Station 模式 + MQTT 客户端 |
| ESP32 WiFi | ESP-NETIF + LwIP | STA 模式，自动重连，事件组同步 |
| ESP32 MQTT | ESP-MQTT (mqtt_client) | QoS 1，TLS 可选，异步事件回调 |
| ESP32 存储 | NVS (Non-Volatile Storage) | 阈值参数持久化 |
| ESP32 外设 | UART、GPIO | UART2 (GPIO16/17) 与 STM32 通信 |
| 通信协议 | MQTT 3.1.1 | Topic：`$sys/{pid}/{device}/thing/property/...` |
| 数据格式 | OneJSON | `{"id":"...","version":"1.0","params":{...}}` |
| 云端平台 | OneNET (中国移动) | 产品 ID：6176tD4mc7，设备名：esp32 |
| MQTT Broker | `studio-mqtt.heclouds.com:1883` | OneNET IoT 平台 MQTT 接入点 |
| 小程序框架 | 微信原生 (WXML + WXSS + JS) | 基础库 3.16.1，AppID：wx72718b58b055cbbb |
| 小程序通信 | `wx.request` HTTPS API | GET/POST 请求 `iot-api.heclouds.com` |
| 小程序鉴权 | Token 鉴权 | version=2018-10-31，method=md5 |
| 小程序状态 | `app.globalData` + `setData` | 全局状态共享 + 页面数据绑定 |
| 小程序样式 | WXSS + CSS 变量 | Flexbox + Grid + 渐变 + 动画 |
| IDE | Keil MDK | STM32 固件编译调试 |
| IDE | VS Code + ESP-IDF 插件 | ESP32 固件编译调试 |
| IDE | 微信开发者工具 | 小程序开发调试 |

---

## 数据流程

### 上行：传感器 → 云平台 → 小程序

```
SHT20 / HC-SR04 / 光敏电阻
        │
        ▼ (I2C / TIM / ADC+DMA)
┌───────────────────┐
│ StartSensorTask   │  每 ~500ms 采集一次
│ (STM32 FreeRTOS)  │
└───────┬───────────┘
        │  UART1 (PA9→GPIO17, 115200 bps)
        │  JSON: {"t":26.5,"h":65,"l":42,"d":8.2}
        ▼
┌───────────────────┐
│ ESP32-S3          │  UART 接收 → 解析 → 重新打包 OneJSON
│ uart_to_mqtt_task │
└───────┬───────────┘
        │  WiFi + MQTT Publish
        │  Topic: $sys/6176tD4mc7/esp32/thing/property/post
        ▼
┌───────────────────┐
│ OneNET 云平台      │  存储 → API 暴露
└───────┬───────────┘
        │  HTTPS GET
        │  /thingmodel/query-device-property?product_id=...&device_name=...
        ▼
┌───────────────────┐
│ 微信小程序         │  每 2 秒轮询 → 更新 UI
└───────────────────┘
```

### 下行：小程序 → 云平台 → 设备

```
┌───────────────────┐
│ 微信小程序         │  用户操作开关 / 调整阈值 / 点击保存
└───────┬───────────┘
        │  HTTPS POST (JSON body)
        │  /thingmodel/set-device-property
        ▼
┌───────────────────┐
│ OneNET 云平台      │  缓存期望值 → MQTT 下发
└───────┬───────────┘
        │  MQTT Subscribe → ESP32
        │  Topic: $sys/6176tD4mc7/esp32/thing/property/set
        ▼
┌───────────────────┐
│ ESP32-S3          │  解析 JSON → UART2 转发
│ handle_cloud_set  │  阈值: {"cmd":"set","thr":{...}}
│                   │  开关: {"cmd":"actuator","led":1,...}
└───────┬───────────┘
        │  UART2 (GPIO16→PA3, 115200 bps)
        ▼
┌───────────────────┐
│ monitorTask       │  解析 JSON → 控制执行器 / 更新阈值
│ (STM32 FreeRTOS)  │
└───────┬───────────┘
        ▼
   LED / 舵机 / 电机 / 蜂鸣器
```

---

## ESP32 MQTT 技术栈详解

ESP32-S3 作为 STM32 与 OneNET 云平台之间的**桥接网关**，运行 ESP-IDF FreeRTOS，负责协议转换与数据转发。

### 核心依赖

| 组件 | 用途 |
|------|------|
| `esp_wifi` + `esp_netif` | WiFi STA 模式初始化、连接管理、事件处理 |
| `lwip` (轻量 IP 协议栈) | TCP/IP 网络层，MQTT 底层传输 |
| `mqtt_client` (ESP-MQTT) | MQTT 3.1.1 客户端，异步事件回调 |
| `nvs_flash` (NVS) | 非易失存储，保存 6 项阈值参数 |
| `cJSON` | JSON 解析与构造 |
| `driver/uart` | UART2 配置 (GPIO16 RX, GPIO17 TX, 115200bps) |

### WiFi 连接流程

```
nvs_flash_init() → esp_netif_init() → esp_event_loop_create()
    → esp_netif_create_default_wifi_sta()
    → esp_wifi_init() → esp_wifi_set_mode(WIFI_MODE_STA)
    → esp_wifi_set_config(SSID="3030", password="3030303030")
    → esp_wifi_start()
    → xEventGroupWaitBits(连接成功事件)
```

WiFi 使用 FreeRTOS 事件组（Event Group）同步：`wifi_event_handler` 在获取 IP 后设置 `WIFI_CONNECTED_BIT`，主任务阻塞等待该事件后再启动 MQTT 连接。

### MQTT 通信机制

```
esp_mqtt_client_init()
    → broker: studio-mqtt.heclouds.com:1883
    → client_id: 6176tD4mc7_esp32_0_0_2025022101
    → username: 6176tD4mc7
    → password: (device key from OneNET)
    → keepalive: 120s
```

MQTT 事件回调 `mqtt_event_handler` 处理：

| 事件 | 行为 |
|------|------|
| `MQTT_EVENT_CONNECTED` | 订阅 `$sys/6176tD4mc7/esp32/thing/property/set`（OneNET 下发命令） |
| `MQTT_EVENT_DATA` | 调用 `handle_cloud_set()` 解析下发的 JSON 命令 |
| `MQTT_EVENT_DISCONNECTED` | 自动重连 |
| `MQTT_EVENT_ERROR` | 日志输出 |

### Topic 设计

| 方向 | Topic | QoS | 说明 |
|------|-------|-----|------|
| 上行 (发布) | `$sys/6176tD4mc7/esp32/thing/property/post` | 1 | 传感器数据上报 |
| 下行 (订阅) | `$sys/6176tD4mc7/esp32/thing/property/set` | 1 | OneNET 下发属性设置命令 |
| 上行 (发布) | `$sys/6176tD4mc7/esp32/thing/property/set_reply` | 1 | 设备回复命令执行结果 |

### 数据转换流程

```
STM32 UART JSON                     ESP32 重新打包 OneJSON
─────────────────────────────────    ─────────────────────────────────────
{"t":26.5,"h":65,"l":42,"d":8.2} →  {"id":"123456",
                                     "version":"1.0",
                                     "params":{
                                       "temp_value":{"value":26.5},
                                       "humidity_value":{"value":65},
                                       "light_value":{"value":42},
                                       "dist_value":{"value":8.2}
                                     }}
```

### NVS 阈值存储

ESP32 在 NVS 中维护 6 个键值对，与 STM32 W25Q64 Flash 保持同步：

| NVS Key | 类型 | 默认值 |
|---------|------|--------|
| `maxtemp_set` | float | 35.0 |
| `minitemp_set` | float | 0.0 |
| `maxhum_set` | int | 80 |
| `minihum_set` | int | 20 |
| `minlight_set` | int | 20 |
| `neardist_set` | float | 10.0 |

### 命令下行解析 (`handle_cloud_set`)

ESP32 收到 OneNET 下发的属性设置 JSON 后：

1. 用 cJSON 解析 `params` 对象中的每个 key-value
2. **阈值属性** (如 `maxtemp_set`)：更新 NVS → 构造 `{"cmd":"set","thr":{...}}` JSON → UART 发送给 STM32
3. **执行器属性** (如 `led_switch`)：更新 NVS → 构造 `{"cmd":"actuator","led":1,...}` JSON → UART 发送给 STM32
4. 发布 `set_reply` 回复 OneNET（含与下发相同的 `id`，`code=200`）

---

## 微信小程序技术栈详解

小程序使用微信原生框架开发，基于基础库 3.16.1，通过 HTTPS API 与 OneNET 云平台通信。

### 项目结构

```
wechat-miniapp/
├── app.js           # 入口文件，globalData 全局状态
├── app.json         # 路由配置 + tabBar 定义 + 窗口样式
├── app.wxss         # 全局样式，CSS 变量定义 (--color-*)
├── utils/
│   ├── onenet.js    # OneNET HTTP API 封装 (3 个导出函数)
│   └── util.js      # 通用工具 (formatTime)
├── pages/
│   ├── dashboard/   # 数据看板 (.js/.wxml/.wxss/.json)
│   ├── control/     # 设备控制
│   ├── threshold/   # 阈值设置
│   └── history/     # 我的（总览）
├── project.config.json
└── sitemap.json
```

### 核心 API — 全局状态 (`app.globalData`)

```javascript
App({
  globalData: {
    productId: '6176tD4mc7',     // OneNET 产品 ID
    deviceName: 'esp32',          // OneNET 设备名
    threshold: { ... },           // 6 项阈值参数
    latestData: null,             // 最新传感器数据缓存
    ledOn: false,                 // LED 状态
    servoOn: false,               // 舵机状态
    motorOn: false                // 风扇状态
  }
})
```

各页面通过 `getApp().globalData` 读写全局状态，实现跨页面数据共享。

### 通信模块 (`utils/onenet.js`)

| 函数 | 请求方式 | Content-Type | 参数位置 | 用途 |
|------|---------|-------------|----------|------|
| `queryDeviceProperties()` | **GET** | — | URL query string | 查询设备最新属性值 |
| `setDeviceProperties(params)` | **POST** | `application/json` | JSON body | 下发属性设置命令 |
| `invokeService(id, params)` | **POST** | `application/json` | JSON body | 调用设备服务 |

**鉴权方式**：在 `Authorization` 请求头中携带预计算 Token：

```
version=2018-10-31&res=products%2F6176tD4mc7%2Fdevices%2Fesp32
  &et=1812468109&method=md5&sign=zSAjzukJabfw%2Ft7tlF%2FtOg%3D%3D
```

其中 `et` 为过期时间戳（设置为 2121 年），`sign` 为设备密钥 + res + et 的 MD5 签名。

### 页面生命周期

```
onLoad (首次加载)
  └─ onShow (每次显示)
       ├─ 读取 globalData 同步状态
       ├─ 调用 onenet API 获取云端数据
       ├─ setData() 更新视图
       └─ setInterval() 启动定时轮询 (2s/次)
           └─ onHide () → clearInterval() 停止轮询
```

### 页面技术对比

| 页面 | 数据来源 | 刷新策略 | 核心 API |
|------|---------|----------|----------|
| 数据看板 | OneNET GET 查询 | `setInterval` 2s 轮询 | `queryDeviceProperties()` |
| 设备控制 | `globalData` 本地状态 | 手动操作 + `onShow` 同步 | `setDeviceProperties()` |
| 阈值设置 | `globalData.threshold` | `onShow` 同步 | `setDeviceProperties()` |
| 我的 | OneNET GET + `globalData` | `onShow` 加载 + 全局缓存 | 综合 |

### UI 技术特性

| 特性 | 实现方式 |
|------|---------|
| 页面布局 | Flexbox + Grid (`display: grid; grid-template-columns: 1fr 1fr`) |
| 主题色系统 | CSS 变量 (`--color-primary`、`--color-temp` 等) |
| 渐变头部 | `linear-gradient()` 各页面独立配色 |
| 动画效果 | `@keyframes`（呼吸脉冲、旋转加载、淡入） |
| 卡片阴影 | `box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.06)` |
| 圆角 | `border-radius: 20rpx` 统一圆角语言 |
| 条件渲染 | `wx:if` / `wx:elif` / `wx:else` |
| 列表渲染 | `wx:for` + `wx:key` |
| 数据绑定 | `{{}}` 双花括号插值 |
| 事件绑定 | `bindtap` / `bindchange` |
| 轮询管理 | `setInterval` / `clearInterval` |

### 设备控制通信链路

```
用户点击 switch
  → onLedToggle(e) → setData({ ledOn: true })
  → app.globalData.ledOn = true
  → sendProp('led_switch', true)
  → onenet.setDeviceProperties({ led_switch: true })
  → wx.request POST /thingmodel/set-device-property
  → OneNET 处理 → MQTT 下发 → ESP32 → STM32 → LED 灯亮
```

控制页采用**乐观更新**策略：先更新本地 UI，再发送云端请求；云端失败仅 Toast 提示，不恢复本地状态。

---

## FreeRTOS 任务架构 (STM32)

| 任务 | 优先级 | 栈大小 | 周期 | 功能 |
|------|--------|--------|------|------|
| `sensorTask` | Normal | 1024B | ~500ms | SHT20 温湿度 + 光敏 + 超声波采集，UART 发送 JSON |
| `displayTask` | Normal | 1024B | 事件驱动 | OLED 菜单渲染，队列消费传感器数据 |
| `inputTask` | Normal | 512B | 10ms 轮询 | 3 按键 + 编码器扫描，自动/手动模式切换 |
| `analysisTask` | Normal | 1024B | 事件驱动 | 阈值比对 → 设置告警标志 → 自动执行器控制 |
| `alarmTask` | Normal | 512B | 事件驱动 | 等待距离告警标志 → 蜂鸣器急促鸣叫 |
| `storageTask` | Low | 1024B | 每分钟 | 传感器数据序列化写入 W25Q64 扇区 1 |
| `monitorTask` | Low | 1024B | 事件驱动 | UART 环形缓冲区解析 ESP32 下发的命令 |
| `bridgeRxTask` | Low | 512B | 轮询 | UART1 DR 寄存器直接读取（独立于 HAL UART） |

---

## 微信小程序页面结构

| Tab | 页面 | 功能 |
|-----|------|------|
| 数据看板 | `dashboard` | 渐变 Hero 时钟 + 2×2 传感器卡片（温/湿/光/距），2 秒自动刷新 |
| 设备控制 | `control` | 开关总览 + LED/舵机/风扇卡片式开关，实时状态条 |
| 阈值设置 | `threshold` | 温度/湿度/其他 3 组滑块，紫色渐变主题，一键下发 |
| 我的 | `history` | 设备信息 + 传感器数据 + 执行器状态 + 阈值总览 |

### 小程序通信模块 (`utils/onenet.js`)

- `queryDeviceProperties()` — GET 请求查询设备最新属性值
- `setDeviceProperties(params)` — POST 请求下发属性设置命令
- `invokeService(id, params)` — POST 请求调用设备服务

鉴权方式：预计算 Token（`version=2018-10-31`，`method=md5`），有效期至 2121 年。

---

## 物模型属性定义

### 传感器属性（只读）

| 标识符 | 名称 | 类型 | 范围 | 单位 |
|--------|------|------|------|------|
| `temp_value` | 当前温度 | float | -40 ~ 125 | °C |
| `humidity_value` | 当前湿度 | int64 | 0 ~ 100 | % |
| `light_value` | 光照强度 | int32 | 0 ~ 100 | % |
| `dist_value` | 测距距离 | float | 0 ~ 400 | cm |

### 阈值属性（读写）

| 标识符 | 名称 | 类型 | 范围 | 单位 |
|--------|------|------|------|------|
| `maxtemp_set` | 温度上限 | float | -40 ~ 125 | °C |
| `minitemp_set` | 温度下限 | float | -40 ~ 125 | °C |
| `maxhum_set` | 湿度上限 | int64 | 0 ~ 100 | % |
| `minihum_set` | 湿度下限 | int64 | 0 ~ 100 | % |
| `minlight_set` | 光照下限 | int32 | 0 ~ 100 | % |
| `neardist_set` | 接近距离 | float | 1 ~ 400 | cm |

### 报警事件

| 标识符 | 名称 | 类型 | 输出参数 |
|--------|------|------|----------|
| `temp_alarm` | 温度报警 | alert | temp_alarm_type (0=下限/1=上限/2=正常) |
| `hum_alarm` | 湿度报警 | alert | hum_alarm_type (0=下限/1=上限/2=正常) |
| `light_alarm` | 光照报警 | alert | light_alarm_type (0=不足/1=正常) |
| `dist_alarm` | 距离报警 | alert | dist_alarm_type (0=过近/1=正常) |

### 执行器属性（读写）

| 标识符 | 名称 | 类型 |
|--------|------|------|
| `led_switch` | LED 开关 | bool |
| `servo_switch` | 舵机开关 | bool |
| `motor_switch` | 电机开关 | bool |

---

## 项目目录结构

```
smart_env_monitor/
├── EnvironmentSense/           # STM32F103C8T6 固件 (Keil + CubeMX)
│   ├── Core/
│   │   ├── Inc/               # 头文件 (sensor_types, app_config, freertos 等)
│   │   └── Src/               # 源文件 (main.c, freertos.c, HAL 文件)
│   ├── Drivers/
│   │   ├── BSP/               # 板级驱动 (sht20, hcsr04, light, actuator,
│   │   │                      #   buzzer, OLED, encoder, menu, w25q64, LED)
│   │   ├── CMSIS/             # ARM CMSIS Core/DSP/NN/RTOS2
│   │   └── STM32F1xx_HAL_Driver/  # STM32 HAL 库
│   ├── MDK-ARM/               # Keil 工程文件
│   └── EnvironmentSense.ioc   # CubeMX 工程
│
├── bridge/                     # ESP32-S3 固件 (ESP-IDF)
│   ├── main/main.c            # 核心桥接代码 (WiFi + MQTT)
│   ├── CMakeLists.txt         # ESP-IDF 构建配置
│   └── build/                 # 编译产物
│
├── wechat-miniapp/             # 微信小程序
│   ├── pages/
│   │   ├── dashboard/         # 数据看板页面
│   │   ├── control/           # 设备控制页面
│   │   ├── threshold/         # 阈值设置页面
│   │   └── history/           # 我的 (总览) 页面
│   ├── utils/
│   │   ├── onenet.js          # OneNET HTTP API 通信模块
│   │   └── util.js            # 通用工具函数
│   ├── app.js                 # 小程序入口 + globalData
│   ├── app.json               # 页面路由 + tabBar 配置
│   └── app.wxss               # 全局样式 (CSS 变量)
│
├── thingmodel-json-import.json # OneNET 物模型定义 (JSON)
├── 项目介绍.md                 # 本文档
├── 智能环境监测系统设计方案.md  # 硬件设计文档
├── ESP32_微信小程序_搭建指南.md # 搭建指南
├── expansion-plan.md           # 执行器扩展计划
├── SHT20-Datasheet.pdf         # SHT20 数据手册
├── HAL_API_Reference.md        # HAL API 参考
└── RTOS_API_Reference.md       # RTOS API 参考
```

---

## 硬件接线总览

### STM32F103C8T6 引脚分配

| 外设 | 引脚 | 功能 |
|------|------|------|
| SHT20 SDA | PB11 | I2C2 数据 |
| SHT20 SCL | PB10 | I2C2 时钟 |
| HC-SR04 Trig | PA0 | GPIO 输出 |
| HC-SR04 Echo | PA1 | TIM2_CH2 输入捕获 |
| 光敏电阻 | PA2 | ADC1_IN2 (DMA) |
| OLED SDA | PB7 | I2C1 数据 |
| OLED SCL | PB6 | I2C1 时钟 |
| 蜂鸣器 | PA8 | GPIO 输出 (低电平触发) |
| LED 灯 | PB5 | GPIO 输出 (高电平开) |
| 舵机 SG90 | PA3 | TIM2_CH4 PWM (50Hz) |
| 电机 TB6612 PWMA | PA11 | TIM1_CH4 PWM (10kHz) |
| TB6612 AIN1 | PB7 | GPIO |
| TB6612 AIN2 | PB8 | GPIO |
| 编码器 A/B/KEY | PB12/PB13/PB14 | GPIO 输入 |
| KEY1/KEY2/KEY3 | PA4/PA5/PA6 | GPIO 输入 |
| W25Q64 CS/SCK/MISO/MOSI | PB0/PB3/PB4/PB5 | SPI2 |
| UART1 TX→ESP32 | PA9 | USART1 |
| UART1 RX←ESP32 | PA10 | USART1 |

### ESP32-S3 引脚分配

| 功能 | 引脚 | 说明 |
|------|------|------|
| UART2 RX←STM32 | GPIO16 | 接收传感器数据 |
| UART2 TX→STM32 | GPIO17 | 发送命令/阈值 |
| WS2812 LED | GPIO48 | 板载 RGB (已关闭) |

---

## 关键技术要点

### SHT20 温湿度驱动

- 基于 I2C2 通信，从机地址 `0x80`
- 内置 CRC-8 校验（多项式 `0x31`），确保数据完整性
- 温度公式：`T = -46.85 + 175.72 × raw / 65536`
- 湿度公式：`H = -6.0 + 125.0 × raw / 65536`（钳位 0~100%）

### HC-SR04 超声波测距

- TIM2 CH2 输入捕获模式，PSC=72 → 1MHz 计数时钟，精度 1µs
- 上升沿记录 CNT → 切换下降沿 → 计算脉宽 → 距离 = 脉宽 / 58
- 支持 32 位溢出自回绕处理

### 光敏电阻

- ADC1_IN2 + DMA1_CH1 循环模式采集 64 点
- 滑动均值滤波消除噪声
- 下拉接法：光照越强 → 电阻越小 → ADC 越小 → 百分比 = 100 - raw×100/4095

### 双端阈值同步

- STM32 W25Q64 Flash 与 ESP32 NVS 同时存储 6 项阈值
- ESP32 启动时从 NVS 读取 → 发送 `{"cmd":"init","thr":{...}}` 给 STM32 恢复
- OneNET 下发阈值 → ESP32 更新 NVS + 转发 STM32 → STM32 写入 W25Q64
- 本地修改阈值 → STM32 写入 W25Q64 + 上报 ESP32 → ESP32 更新 NVS

### 自动/手动模式

- 默认自动模式：`analysisTask` 根据传感器值与阈值比对，自动控制执行器
- 手动模式：通过小程序或按键控制，由 `monitorTask` 解析下发命令
- 长按 KEY3 切换模式，OLED 弹出全屏提示 1.2 秒

---

## 版本信息

- 项目版本：1.2
- STM32 固件：CubeMX + HAL + FreeRTOS CMSIS_V2
- ESP32 固件：ESP-IDF v5.5.4
- 小程序基础库：3.16.1
- OneNET API 版本：2018-10-31
